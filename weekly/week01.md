# Week 1 Quiz — Python Foundations

Answer these without looking anything up.

1. What happens when you `import` a module?
2. What is the difference between a script and a module?
3. What does `__name__` represent?
4. What does `if __name__ == "__main__"` prevent?
5. What is a namespace?
6. What is pip actually doing?
7. Why are virtual environments necessary?
8. What happens when Python encounters an exception?
9. What is the difference between a class and a function?
10. What does Python do when you run `python file.py`?
11. Where does Python look when importing a module?
12. Why is separating code into modules useful?

Write answers in this file.
